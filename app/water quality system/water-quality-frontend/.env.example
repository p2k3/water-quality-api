# Example environment variables for the Vite frontend
# Rename to .env.local for local development (do NOT commit .env.local)
# Set the backend API base URL (use HTTPS in production)
VITE_API_URL=https://your-backend.example.com
